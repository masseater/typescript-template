name: can-not-now
description: A small task left for the scheduled can-not-now run to turn into a pull request
labels: [can-not-now]
body:
  - type: textarea
    id: task
    attributes:
      label: What to change
    validations:
      required: true
  - type: textarea
    id: done
    attributes:
      label: Done when
    validations:
      required: true
