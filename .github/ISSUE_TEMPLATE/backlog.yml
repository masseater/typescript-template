name: backlog
description: A small task left for the scheduled backlog run to turn into a pull request
labels: [backlog]
body:
  - type: textarea
    id: task
    attributes:
      label: What to change
    validations:
      required: true
  - type: textarea
    id: done
    attributes:
      label: Done when
    validations:
      required: true
