# 変更量を理由にした検証の回避

- 変更量・ファイル数・変更範囲を理由に、lint・typecheck・test・build・security check の対象を減らさない。「変更が多い場合はスキップ」「大量変更は対象外」といった allowlist・denylist・閾値を作らない。
- 特定のファイル・workspace・feature を、変更量や対応コストを理由に検証対象から外さない。
- 既にある allowlist や例外は必要性を確かめ、不要なら削除する。必要でも、変更量を理由にした恒久的な回避にしない。
- 検証量が問題なら、対象を削らずに task graph・依存関係・cache・並列実行で効率化する。大規模変更自体がボトルネックなら、計測して原因を特定してから改善する。
- 判断は「その例外がなければ、どの検証を本来実行すべきか」で決める。変更量以外に仕様上の理由が無い例外は削除する。
