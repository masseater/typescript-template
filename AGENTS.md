# AGENTS.md

MUST は RFC 2119 の意味で解釈する。

- MUST: コードの簡潔さや様式より、機械検査による問題検出、AI による実アプリの直接操作、AI による実測データの直接照会を優先する。
- IF: 動作を確認する; THEN MUST: agent-browser で実アプリを操作し、その操作に対応する実ログとトレースを、ローカルでは Cloudflare Local Explorer、本番では Workers Observability から直接取得する。
- MUST: 観測は Cloudflare と Pulumi だけで完結させる。外部の観測 SaaS やコンテナの観測基盤を追加しない。
- PROHIBIT: プロセス起動・疎通・コード読解だけを根拠に、機能や観測性が動くと判定する。
- MUST: 関連アプリ・共通パッケージ・インフラ・内部運用ツールをこの単一リポジトリで管理する。
- MUST: 一般ユーザー用と管理者用の実行・配信・権限を分離する。共有パッケージはアプリに依存させない。
- IF: 機能を追加する; THEN MUST: 品質検査と観測性を先に用意する。
- MUST: テストでは実 DB と実サービスを使用する。外部 HTTP の置換に限り MSW を使う。
- PROHIBIT: 内部モジュール・関数・DB のモック、権限制御の迂回、Git フックのスキップを行う。
- PROHIBIT: 認証情報・確認トークン・プロフィール本文をログまたはブラウザ配信物に漏らす。
- IF: 実行・調査コマンドを追加する; THEN MUST: リポジトリの package.json に入口を定義し、秘密情報を出力せず構造化された結果を返す。
- IF: 自動検出できる制約を追加する; THEN MUST: 規約の文章だけでなく lint またはテストで違反を検出する。
- IF: ユーザーから別の指示がない; THEN MUST: リポジトリ本体の main で作業し、変更をコミットして main へ直接 push する。PR と worktree の作成は不要とする。
