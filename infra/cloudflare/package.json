{
  "name": "@template/infra-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "verify:artifacts": "node src/check-artifacts.ts",
    "verify:stacks": "node src/check-stacks.ts",
    "preview": "node src/cli.ts plan",
    "deploy": "node src/cli.ts deploy",
    "db:migrate:remote": "node src/database-command.ts migrate",
    "db:bootstrap:remote": "node src/database-command.ts bootstrap"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@template/budget-monitor": "workspace:*",
    "@template/config": "workspace:*",
    "@template/db": "workspace:*",
    "@template/error-monitor": "workspace:*",
    "@template/health-monitor": "workspace:*",
    "alchemy": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@effect/vitest": "catalog:",
    "@types/node": "catalog:",
    "msw": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
