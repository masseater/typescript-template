import { SignOutButton } from "./sign-out-button";
import preview from "../.storybook/preview";

const meta = preview.meta({ component: SignOutButton });

const Default = meta.story();

export { Default };
